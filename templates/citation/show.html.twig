{% extends 'base.html.twig' %}

{% block title %}Vos Citations sauvegardé{% endblock %}

{% block body %}
    <div class="d-flex row mt-5">
        {% for citation in citations %}
            <div class="m-1 p-1 border d-flex justify-content-between align-items-center">
                <div>
                    <h4>{{ citation.quote }}</h4>
                    <p>{{ citation.caractere }}</p>
                </div>
                <div>
                    <a class="btn btn-danger" href="{{ path('delete_quote_by_user', {id:citation.id}) }}">delete</a>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
